import EmailVerificationForm from '@/app/[locale]/(auth)/verify-email/verify-form';
import React from 'react';

const VerifyEmail = () => {
	return (
		<div>
			<EmailVerificationForm />
		</div>
	);
};

export default VerifyEmail;
