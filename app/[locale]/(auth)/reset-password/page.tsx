import ResetPassword from '@/app/[locale]/(auth)/reset-password/reset-form';
import React from 'react';

const ResetPasswordPage = () => {
	return (
		<div>
			<ResetPassword />
		</div>
	);
};

export default ResetPasswordPage;
